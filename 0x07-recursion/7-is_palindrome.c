#include "holberton.h"
#include <stdio.h>

/**
 * palindromo - Do the square roo.t
 *
 * @n: Argument for the function.
 * @i: Argument for the function.
 *
 * Return: 1 returns i or -1 depending which.
 */
int palindromo(char *s, int i)
{
}
/**
 * reverse - Do the square roo.t
 *
 * @n: Argument for the function.
 * @i: Argument for the function.
 *
 * Return: 1 returns i or -1 depending which.
 */
int reverse(char *s, char *rev)
{
}
/**
 * is_palindrome - Returns the natural square root of a number.
 *
 * @s: Argument for the function.
 *
 * Return: 1 returns result.
 */
int is_palindrome(char *s)
{
	int result;
	char rev

	result = raiz(n, 1);
	return (result);
}
