#include "holberton.h"
#include <stdio.h>

/**
 * set_string - prints buffer in hexa
 * @s: argument string
 * @to: set of bytes to be searched
 *
 * Return: an array.
 */
void set_string(char **s, char *to)
{
to = **s;
}
